# The fight registry — the ONLY hand-edited data file (spec §10).
# role: hero (full CV) | proleague (results/attention) | corpus (cheap-lane scorecard)
#
# Filling yt_id / fight_start_s / fight_end_s is a HUMAN task (spec §9.1).
# `bb fetch` is human-triggered; never auto-download in tests or CI (§9.2).

fights:
  # --- Synthetic. Always present. Needs no network and no footage. ---
  - {fight_id: fixture-001, episode: 0, bots: [Minotaur, Bloodsport], role: hero, synthetic: true}

  # --- Hero fights (full CV lane) ---
  # HUGE is the easiest CV target in the sport — big, slow, high contrast.
  - {fight_id: pl-e01-f2, episode: 1, bots: [HyperShock, HUGE],     role: hero, yt_id: TODO, fight_start_s: TODO, fight_end_s: TODO}
  # Hazard drama per the official teasers.
  - {fight_id: pl-e04-f2, episode: 4, bots: [Bloodsport, Minotaur], role: hero, yt_id: TODO, fight_start_s: TODO, fight_end_s: TODO}

  # --- Pro League (results + attention only, no tracking) ---
  - {fight_id: pl-e01-f1, episode: 1, bots: [DeathRoll, Golden Fury],  role: proleague, yt_id: TODO}
  - {fight_id: pl-e01-f3, episode: 1, bots: [End Game, Malice],        role: proleague, yt_id: TODO}
  - {fight_id: pl-e04-f1, episode: 4, bots: [Orbitron, Witch Doctor],  role: proleague, yt_id: TODO}
  - {fight_id: pl-e04-f3, episode: 4, bots: [Cobalt, The Twins],       role: proleague, yt_id: TODO}

  # Episodes 2–3 fight cards: fill from battlebots.fandom.com episode pages.
  # C3 (sources/wiki.py) can auto-suggest patches once it lands.

  # --- Corpus: 8–12 judges'-decision fights, 2021–2023, footage on the official
  # channel. Prefer fan-alleged robberies — they are the Robbery Leaderboard.
  # Human fills. ---
