"""External data sources. Every module here emits §5 shapes and nothing else.

Rules-adaptation seam (§8): if the 17:00 rules mandate a specific dataset or API,
a new source module goes here emitting the same FightMeta / CSV shapes. Nothing
downstream changes.
"""
